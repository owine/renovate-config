{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Python ecosystem groupings (pep621/uv/poetry). Most rules bundle peer-dep or hard-pinned stacks that must move together (boto3/botocore is the strictest); scientific-python is explicitly churn reduction rather than break prevention. Each rule states which it is. `sentry-sdk` DELIBERATELY HAS NO RULE (decided 2026-09-25, ahead of trip-tracker and youth-activity-scheduler adopting it). It is ONE PyPI package whose integrations are extras (`sentry-sdk[fastapi]`), so a group would have one member. Its extras declare only floors (`fastapi>=0.79.0`, `starlette>=0.19.1`, `celery>=3`) and it detects framework versions at runtime, so there is no coupling to `fastapi-stack` or `celery` to protect. If a rule is ever added, scope it `matchManagers: [pep621]`, because npm has an unrelated `sentry-sdk` package (the `vine`/`playwright` hazard). Name it `sentry-python`, never `sentry`: that is node.json's group name, and a shared groupName merges both ecosystems onto one branch (the `opentelemetry-python`/`opentelemetry-js` convention).",
  "pep621": { "enabled": true },
  "packageRules": [
    {
      "description": "FastAPI + ASGI stack (web layer)",
      "matchManagers": ["pep621"],
      "matchPackageNames": [
        "fastapi",
        "uvicorn",
        "starlette",
        "jinja2",
        "httpx",
        "itsdangerous",
        "structlog",
        "authlib",
        "python-multipart"
      ],
      "groupName": "fastapi-stack"
    },
    {
      "description": "Pydantic + pydantic-settings (released together)",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["pydantic", "pydantic-settings"],
      "groupName": "pydantic"
    },
    {
      "description": "SQLAlchemy + drivers + Alembic (data layer)",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["sqlalchemy", "alembic", "asyncpg", "psycopg", "aiosqlite"],
      "groupName": "sqlalchemy-stack"
    },
    {
      "description": "pytest + plugins + respx (test infra)",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["pytest", "/^pytest-/", "respx"],
      "groupName": "pytest-stack"
    },
    {
      "description": "Lint + type tooling (ruff/mypy/bandit/pip-audit/djlint/types-*/pre-commit)",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["ruff", "mypy", "bandit", "pip-audit", "djlint", "/^types-/", "pre-commit"],
      "groupName": "lint + types tooling"
    },
    {
      "description": "boto3 + botocore + the transfer/async siblings. botocore is HARD-PINNED by boto3 to a narrow range (boto3 1.x.y requires botocore >=1.3x.y,<1.3x.y+1), so a split major is not a drift risk — it is an immediate unsatisfiable-resolution failure on whichever half lands first. The strongest coupling in this file. `aiobotocore` and `s3transfer` pin botocore the same way. NAMED `boto3`, NOT `aws-sdk` — do NOT rename it to match node.json's AWS group. A groupName is a branch name, so two rules sharing one name share one PR even across managers and preset files; since youth-activity-scheduler and claude-terminal-home-assistant extend BOTH :node and :python, a shared `aws-sdk` name would merge unrelated @aws-sdk/* and boto3 updates into a single cross-manager PR. The `matchManagers` scope below does NOT prevent this: it controls which deps MATCH, not which branch they land on.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["boto3", "botocore", "aiobotocore", "s3transfer"],
      "groupName": "boto3"
    },
    {
      "description": "PyTorch trio — torchvision and torchaudio are compiled against an exact torch version and are published per torch release; a split leaves a binary extension linked to a torch ABI that is no longer installed, which surfaces as an import-time symbol error rather than a resolver failure.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["torch", "torchvision", "torchaudio"],
      "groupName": "pytorch"
    },
    {
      "description": "LangChain — `langchain`, `langchain-core`, and the `langchain-*` provider packages all pin langchain-core and move on one release cadence; the core interfaces change across minors, so even a non-major skew can break provider imports.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["langchain", "/^langchain-/", "langsmith"],
      "groupName": "langchain"
    },
    {
      "description": "OpenTelemetry Python — api/sdk/exporters/instrumentation share a registered global provider and are published as one release set; mixing versions silently drops spans. Mirrors the `/^@opentelemetry//` rule in node.json in intent, but the group name is deliberately DIFFERENT from it (`opentelemetry-js` there) — see the `boto3` rule above for why a shared name would merge the two ecosystems onto one branch.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["/^opentelemetry-/"],
      "groupName": "opentelemetry-python"
    },
    {
      "description": "Celery + its transport/execution stack — kombu, billiard, and vine are cut per celery release and pinned narrowly by it. NOTE the `matchManagers: [pep621]` scope is load-bearing here beyond this file's convention: `vine` is ALSO a real npm package name, and a consumer extending both :node and :python would otherwise have an unrelated JS package pulled into the Celery group — the same cross-ecosystem accident the playwright rule in node.json guards against with matchDatasources.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["celery", "kombu", "billiard", "vine"],
      "groupName": "celery"
    },
    {
      "description": "PRE-SEEDED. Django + DRF + the django-* plugin ecosystem: plugins declare their supported Django major and are re-cut per Django LTS, so a core major without its plugins fails at app-registry load. The `/^django-/` regex covers third-party plugins, which is acceptable here (unlike the tailwind-merge case in node.json) because the django-* prefix is a community convention specifically for Django-version-coupled packages.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["django", "djangorestframework", "/^django-/"],
      "groupName": "django"
    },
    {
      "description": "PRE-SEEDED. Hugging Face stack — transformers pins tokenizers to a narrow range and both track huggingface-hub's API; datasets and accelerate follow the same cadence.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["transformers", "tokenizers", "huggingface-hub", "datasets", "accelerate"],
      "groupName": "huggingface"
    },
    {
      "description": "PRE-SEEDED. numpy + scipy + pandas. CHURN REDUCTION, NOT BREAK PREVENTION — stated plainly so the justification is not misread: these are independent projects and a split major usually resolves fine. They are grouped only because scipy and pandas compile against numpy's C ABI, so a numpy major is the event that drags the other two, and reviewing them on three branches hides that. Do NOT extend this rule to the wider scientific stack on the same reasoning.",
      "matchManagers": ["pep621"],
      "matchPackageNames": ["numpy", "scipy", "pandas"],
      "groupName": "scientific-python"
    },
    {
      "description": "Don't track `python` via the native pep621 manager — `requires-python` is normally a min-version metadata range (`>=3.10`), not a runtime pin, so the pep621 view of it must not bump. Exact `==X.Y.Z` pins ARE tracked, but via the custom manager below + the `python-version` datasource, which surfaces a separate `python` dep that this rule does not touch.",
      "matchManagers": ["pep621"],
      "matchDepNames": ["python"],
      "enabled": false
    },
    {
      "description": "Group the exact-pinned `requires-python` (custom manager below, depName `python`) into the same PR as the `python` Docker base image (dockerfile manager, also depName `python`) so an exact CPython pin and its base image never bump separately and drift apart. Split, NEITHER half can go green: the base-image-only PR fails `No interpreter found for Python <old>` (pyproject still pins the old triple, and UV_PYTHON_DOWNLOADS=never forces the mismatch to fail loudly) and the pyproject-only PR fails `The current Python version is not compatible with the locked Python requirement` — owine/trip-tracker #180/#182. Both spellings are listed because `FROM docker.io/library/python:X` yields the fully-qualified depName rather than the bare `python`, mirroring docker.json's runtime-major label rule; plain strings are glob-matched with `.`/`/` literal, so these match exactly and leave `python-slim`/`my.registry/python` alone. This holds regardless of `extends` order ONLY because docker.json's `dockerfile bases` rule negates these same two names — that rule matches `matchManagers: [dockerfile]`, so with :python extended before :docker it would otherwise win the groupName and pull the base image back out. Keep this list and that negation in sync. MAJORS: intentionally no `matchUpdateTypes` — the group applies to majors too, because a runtime major has exactly the same drift hazard as a patch and the two deps always bump to the same version. This does not weaken the fleet's major policy: default.json's `separateMajorMinor`/`separateMultipleMajor` still isolate the major onto its own branch, and its major rule still applies the 7-day soak, `automerge: false`, `feat:` type, and `major-update`/`needs-review` labels. Grouping only decides which deps share that branch, not how it is reviewed.",
      "matchPackageNames": ["python", "docker.io/library/python"],
      "groupName": "python runtime"
    }
  ],
  "customManagers": [
    {
      "description": "Track an EXACT-pinned `requires-python = \"==X.Y.Z\"` in pyproject.toml against the CPython runtime (datasource `python-version`, depName `python`) so it bumps in lockstep with the `python` Docker base image. Tolerant of formatting: single OR double TOML quotes and optional whitespace around `=`/`==` all match; a trailing inline comment after the closing quote is unaffected since the match ends at the quote. Anchored to the `==`-triple form only: floor ranges (`>=3.10`), compatible ranges (`~=3.14`), and wildcard pins (`==3.14.*`) do NOT match, so non-exact-pin consumers are a no-op. The `==` is literal context outside the capture; only the `X.Y.Z` triple is replaced. CONFLICT RESOLUTION: if the system Python ever fails to satisfy this exact pin, uv silently falls back to a uv-managed standalone interpreter (unreachable by a non-root runtime user) — consumers MUST set `UV_PYTHON_DOWNLOADS=never` so that mismatch fails the build loudly instead. MANUAL STEP REQUIRED — a `python runtime` PR lands RED and needs one hand-authored `uv lock` commit pushed to its branch before CI can pass. uv.lock carries its OWN `requires-python`, and nothing in this preset can refresh it: a custom regex manager performs a text substitution and runs no lockfile update, and `postUpgradeTasks` (which could shell out to `uv lock`) is unavailable on the Mend-hosted app this fleet uses. Until that commit is pushed, every uv job fails `The current Python version (<new>) is not compatible with the locked Python requirement: ==<old>`. Lockfile maintenance does eventually reconcile uv.lock, but on its own mon/wed/fri/sun schedule and on a DIFFERENT branch — it will not unblock the open PR.",
      "customType": "regex",
      "managerFilePatterns": [
        "/(^|/)pyproject\\.toml$/"
      ],
      "matchStrings": [
        "requires-python[ \\t]*=[ \\t]*['\"]==[ \\t]*(?<currentValue>\\d+\\.\\d+\\.\\d+)['\"]"
      ],
      "datasourceTemplate": "python-version",
      "depNameTemplate": "python",
      "versioningTemplate": "python"
    }
  ]
}
