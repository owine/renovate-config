{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "owine/* baseline — supply-chain hardened defaults. Pin everything, soak releases, escalate CVEs.",
  "extends": [
    "config:best-practices",
    "helpers:pinGitHubActionDigests",
    "security:openssf-scorecard",
    ":dependencyDashboard",
    ":semanticCommits"
  ],
  "timezone": "America/Chicago",
  "schedule": ["on monday"],
  "automergeSchedule": ["at any time"],
  "labels": ["dependencies"],
  "semanticCommitType": "deps",
  "semanticCommitScope": null,
  "configMigration": true,
  "pre-commit": { "enabled": true },
  "rebaseWhen": "auto",
  "rangeStrategy": "pin",
  "separateMajorMinor": true,
  "separateMultipleMajor": true,
  "prHourlyLimit": 0,
  "prConcurrentLimit": 0,
  "minimumReleaseAge": "3 days",
  "minimumReleaseAgeBehaviour": "timestamp-optional",
  "osvVulnerabilityAlerts": true,
  "dependencyDashboardOSVVulnerabilitySummary": "all",
  "vulnerabilityAlerts": {
    "labels": ["security"],
    "minimumReleaseAge": "0 days",
    "schedule": ["at any time"],
    "automerge": true,
    "semanticCommitType": "fix",
    "groupName": "security updates"
  },
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["on monday, wednesday, friday and sunday"],
    "minimumReleaseAge": "0 days",
    "automerge": true
  },
  "packageRules": [
    {
      "description": "Reclaim `deps:` as the baseline commit type. The top-level `semanticCommitType: deps` above is INERT on its own: Renovate ships two built-in default packageRules — `{matchPackageNames:['*'] => chore}` and `{matchDepTypes:['dependencies','require'] => fix}` — and packageRules ALWAYS beat top-level config, so without this override every bump commits as `chore:` (npm prod deps as `fix:`). That matters because release-please treats `chore:` as a HIDDEN, non-releasing type: Dockerfile/Actions/apk repos would cut zero releases and add nothing to the changelog. This catch-all re-forces `deps:` for every package (release-please recognizes `deps` as a releasable unit => patch, in a dedicated Dependencies changelog section). Ordered FIRST so the more specific rules below/after still win where they match: lock file maintenance => chore, CI/Actions => ci, security => fix (vulnerabilityAlerts, force). Nothing in this fleet types a dependency update `feat:` — majors included; see the major rule below.",
      "matchPackageNames": ["*"],
      "semanticCommitType": "deps"
    },
    {
      "description": "Lock file maintenance commits as `chore:` — the ONE update type that must NOT cut a release. It refreshes transitive/nested deps within existing manifest ranges (no direct-dependency change consumers can see) and runs 4x/week (mon/wed/fri/sun, see the lockFileMaintenance block). Under the `deps:` baseline above it is a release-please releasable unit => a patch release every refresh = pure changelog/version spam. `chore:` is release-please's HIDDEN, non-releasing type, so refreshes stay silent. Uses matchUpdateTypes:['lockFileMaintenance'] (not matchPackageNames) because the maintenance branch has no depName; ordered AFTER the catch-all so it wins over `* => deps`.",
      "matchUpdateTypes": ["lockFileMaintenance"],
      "semanticCommitType": "chore"
    },
    {
      "description": "Major bumps — separate PR per package, 7-day soak, manual review required. Deliberately sets NO semanticCommitType: it falls through to the `* => deps` catch-all above and cuts a PATCH release, in the changelog's Dependencies section alongside routine churn. This rule used to type majors `feat:` (minor release, Features section); that is gone, because `feat:` should mean a feature someone here wrote, not an upstream version number crossing a line — inflating consumers' minor versions on dependency churn made the minor digit meaningless. Nothing Renovate generates is `feat:` any more. What still marks a major as special is the review surface, not the commit type: its own branch (separateMajorMinor/separateMultipleMajor), a 7-day soak, automerge off, and the `major-update`/`needs-review` labels. Also still not `feat!:`/breaking: an upstream major is not automatically the consumer's breaking change.",
      "matchUpdateTypes": ["major"],
      "minimumReleaseAge": "7 days",
      "automerge": false,
      "addLabels": ["major-update", "needs-review"]
    },
    {
      "description": "CI plumbing commits as `ci:` — release-please's HIDDEN, non-releasing type. Actions, reusable workflows and composite `action.yml` files are build-time infrastructure: nothing a consumer of the published image/package/server can observe, so bumping actions/checkout should neither cut a patch release nor add a changelog line. Ordered AFTER the `* => deps` catch-all and the major rule so it wins for every update type, major included — a GHA major still gets that rule's 7-day soak, `automerge: false` and `major-update`/`needs-review` labels, it just doesn't release. Security is exempt automatically: `vulnerabilityAlerts.semanticCommitType: fix` is applied at force level and beats every packageRule, so a CVE in an action still lands in Bug Fixes and cuts a release. Covers `.github/workflows`, `.github/actions`, `workflow-templates/` and any `action.yml` — the github-actions manager's own file patterns.",
      "matchManagers": ["github-actions"],
      "semanticCommitType": "ci"
    },
    {
      "description": "Same `ci:` treatment for the workflow version literals the customManagers block below extracts — annotated tool versions in matrix arrays, run-step args and env defaults, AND the non-literal runner labels (`matrix.include[].runner`, `workflow_call` input defaults, JSON-in-`run:`) picked up by the second manager — all inside `.github/workflows/*.yml`. Those deps belong to the custom regex manager, not `github-actions`, so the matchManagers rule above can't reach them; they are matched by path instead. This is what keeps a runner-label bump typed `ci:` and consistent with the literal `runs-on:` half of the very same PR. Deliberately NOT `matchManagers: ['custom.regex']`, which would also silence any unrelated custom manager a consumer repo defines.",
      "matchFileNames": [".github/workflows/**"],
      "semanticCommitType": "ci"
    },
    {
      "description": "Pin Docker image and GitHub Action digests so a re-tagged image/action can't slip in",
      "matchManagers": ["dockerfile", "github-actions"],
      "pinDigests": true
    },
    {
      "description": "docker-compose: bump tags but never pin digests — keeps compose files readable for humans editing them by hand",
      "matchManagers": ["docker-compose"],
      "pinDigests": false
    },
    {
      "description": "Node version pins use LTS-aware versioning — Renovate's 'node' versioning scheme tracks LTS lines (22, 24, 26, ...) and skips Current/odd-numbered releases (23, 25, 27, ...). LTS-to-LTS major bumps still come as separate manual-review PRs via the major-bump rule above.",
      "matchPackageNames": ["node"],
      "matchDatasources": ["docker", "node-version"],
      "versioning": "node"
    }
  ],
  "customManagers": [
    {
      "description": "Bump version literals in GitHub Actions workflows annotated with `# renovate: datasource=X depName=Y`. Supports trailing-comment (hint after value) and leading-comment (hint on line above value) styles. INVARIANT: inter-field separators must use `[ \\t]`, never `\\s` — `\\s` matches `\\n` and causes cross-block attribution (see PR #4). The two patterns share textually identical metadata-tail and value subpatterns; JSON has no interpolation and RE2 has no subpattern references, so edits must apply character-for-character to both. Reaches places the github-actions manager can't natively (matrix arrays, run-step args, env defaults).",
      "customType": "regex",
      "managerFilePatterns": ["/^\\.github/workflows/[^/]+\\.ya?ml$/"],
      "matchStrings": [
        "['\"]?(?<currentValue>\\d+\\.\\d+\\.\\d+)['\"]?[ \\t]*#[ \\t]*renovate:[ \\t]*datasource=(?<datasource>\\S+)[ \\t]+depName=(?<depName>\\S+)(?:[ \\t]+packageName=(?<packageName>\\S+))?(?:[ \\t]+versioning=(?<versioning>\\S+))?",
        "#[ \\t]*renovate:[ \\t]*datasource=(?<datasource>\\S+)[ \\t]+depName=(?<depName>\\S+)(?:[ \\t]+packageName=(?<packageName>\\S+))?(?:[ \\t]+versioning=(?<versioning>\\S+))?[ \\t]*\\n[^\\n]*?['\"]?(?<currentValue>\\d+\\.\\d+\\.\\d+)['\"]?"
      ]
    },
    {
      "description": "Make runner labels visible where they are NOT a literal `runs-on:` value. Renovate's github-actions manager extracts runners from literal `runs-on:` ONLY (its docs say so outright for env vars), so a label reached indirectly is invisible — and merging Renovate's partial PR then ships a SPLIT-BRAIN build (some jobs on the new image, some on the old) that stays GREEN, because both images work. Three shapes are covered, all observed in owine/*: (a) `matrix.include[]` entries `runner: ubuntu-26.04` — doc-scanner, youth-activity-scheduler, nut-cgi, MLB-Deferred-Contract-Calculator, house-manager; (b) a reusable-workflow `workflow_call` input named `runner` whose `default:` carries the label — compose-workflow, which today has 3 such defaults alongside 4 literal `runs-on:` and is one Renovate PR away from split-brain; (c) the same key inside a JSON string in a `run:` step, `\"runner\":\"ubuntu-26.04\"` — trip-tracker's dynamically-built matrix. INVARIANT — the capture must mirror the built-in manager's own regex `^\\s*(?<depName>[a-zA-Z]+)-(?<currentValue>[^\\s]+)`: `currentValue` takes the WHOLE remainder, suffix included, because the github-runners datasource carries `26.04-arm`, `15-large` and `15-intel` as distinct versions — `24.04-arm` is NOT `24.04` with decoration. Capturing a bare `\\d+\\.\\d+` would file the arm runner under the x64 runner's version and could propose a label that does not exist for arm. Matching the built-in extraction byte-for-byte also makes these deps collapse into the SAME branch as the literal `runs-on:` ones, which is what turns two half-PRs into one whole one. No `autoReplaceStringTemplate` on purpose: the default value-only replacement rewrites exactly the `currentValue` span. No `versioningTemplate` either — the github-runners datasource defaults to `docker` versioning, same as the built-in manager. INVARIANT — inter-field separators use `[ \\t]`, never `\\s`, so a match cannot leak across lines (see PR #4); `\\s` appears only inside negated classes as a value terminator, which is the opposite concern. The multi-line pattern (b) is bounded to <=6 intervening lines that each carry a `key: value` pair, so it stops at the next bare block-opening key and cannot latch onto a sibling input's `default:`; it also cannot match the `description:` line that quotes a sample label, because `default:` is required immediately before the value. These deps belong to `custom.regex`, not `github-actions`, so they are typed `ci:` by the `matchFileNames: ['.github/workflows/**']` rule above, not by the `matchManagers` one.",
      "customType": "regex",
      "managerFilePatterns": ["/^\\.github/workflows/[^/]+\\.ya?ml$/"],
      "matchStringsStrategy": "any",
      "matchStrings": [
        "[\"']?runner[\"']?:[ \\t]*[\"']?(?<depName>ubuntu|windows|macos)-(?<currentValue>[0-9][^\\s\"',\\]}]*)",
        "runner:[ \\t]*\\n(?:[ \\t]+[a-z-]+:[ \\t]*\\S[^\\n]*\\n){0,6}?[ \\t]+default:[ \\t]*[\"']?(?<depName>ubuntu|windows|macos)-(?<currentValue>[0-9][^\\s\"',\\]}]*)"
      ],
      "datasourceTemplate": "github-runners"
    }
  ]
}
